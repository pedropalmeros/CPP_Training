/*************************************************************************************************************************
 * Pinters can be added or substracted betweenthen, this is a powerful technique that is often used with array indexing. 
 ************************************************************************************************************************/

#include <iostream> 
#include <ctype.h> 
#include <string> 

using namespace std; 

// MAIN PROGRAM - CONTROLLER
int main()
{
    const int bufferSize = 255; 
    char buffer[ bufferSize + 1] ;
    char word[bufferSize + 1]; 
    int wordDisplacement = 0;

    return 0;
}